const allLiTags = document.querySelectorAll('nav.navigation li');
console.log(allLiTags.length);
const allParfs = document.querySelectorAll('div p');
console.log(allParfs.length);
const allDivs = document.querySelectorAll('article div');
console.log(allDivs.length);
